<% if model.errors.any? %>

    <div class= "alert alert-danger">
      <h3><%= pluralize(model.errors.count, "error") %> prohibited this from being saved:</h3>
      <ul>
        <% model.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>
